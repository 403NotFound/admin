const getters = {
  token: (state) => state.token,
  avatar: (state) => state.avatar,
  name: (state) => state.name,
  roles: (state) => state.roles,
  permission_routes: (state) => state.routes,
  sidebar_opend: (state) => state.sidebarOpend,
}
export default getters
